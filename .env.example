# Example environment variables for local development
# Note: sticky bar messages are now defined directly in code (client/src/App.tsx).
# To change messages, edit the `stickyMessages` array in that file and restart the dev server.
